# سیستم مدیریت محتوا با ادیتور گرافیکی

این یک سیستم کامل مدیریت محتوا با ادیتور گرافیکی پیشرفته است که به شما امکان می‌دهد پست‌های گرافیکی جذاب ایجاد کنید.

## ویژگی‌ها

### 🔧 ادیتور گرافیکی
- **کانواس تعاملی**: طراحی گرافیکی با drag & drop
- **عناصر متن**: اضافه کردن و ویرایش متن با فونت‌های مختلف
- **اشکال هندسی**: مربع، دایره و اشکال دلخواه
- **پس‌زمینه**: رنگ یا تصویر با قابلیت تار کردن
- **لایه‌بندی**: مدیریت لایه‌ها و تغییر ترتیب آن‌ها
- **قفل کردن**: قفل کردن عناصر برای جلوگیری از تغییرات ناخواسته

### 📱 حالت کاربر
- مشاهده پست‌های منتشر شده
- پیش‌نمایش گرافیکی از محتوای پست‌ها
- نمایش اطلاعات نویسنده و تاریخ انتشار

### ⚙️ حالت ادمین
- ایجاد پست جدید با ادیتور گرافیکی
- ویرایش پست‌های موجود
- انتشار یا پیش‌نویس کردن پست‌ها
- آرشیو و حذف پست‌ها
- مدیریت کامل محتوا

## نحوه استفاده

### ۱. ایجاد پست جدید
1. به تب **حالت ادمین** بروید
2. روی دکمه **ساخت پست جدید** کلیک کنید
3. عنوان و توضیحات پست را وارد کنید
4. با استفاده از ادیتور گرافیکی، محتوای خود را طراحی کنید

### ۲. کار با ادیتور گرافیکی
- **افزودن عناصر**: از دکمه‌های بالا برای افزودن متن، مربع یا دایره استفاده کنید
- **انتخاب عنصر**: روی هر عنصر کلیک کنید تا انتخاب شود
- **جابجایی**: عناصر را با ماوس به دلخواه جابجا کنید
- **تغییر اندازه**: گوشه پایین راست هر عنصر را بکشید تا اندازه آن تغییر کند
- **ویرایش متن**: روی متن دابل کلیک کنید و آن را ویرایش نمایید

### ۳. تنظیمات پس‌زمینه
- **رنگ پس‌زمینه**: از انتخابگر رنگ استفاده کنید
- **تصویر پس‌زمینه**: URL تصویر مورد نظر را وارد کنید
- **میزان تاری**: با اسلایدر میزان تاری پس‌زمینه را تنظیم کنید

### ۴. مدیریت لایه‌ها
- **تغییر ترتیب**: با دکمه‌های بالا/پایین ترتیب لایه‌ها را تغییر دهید
- **نمایش/مخفی**: با چشمک هر لایه را نمایش یا مخفی کنید
- **قفل کردن**: با قفل هر لایه را در مقابل تغییرات محافظت کنید

## ساختار فایل‌ها

```
src/
├── app/
│   ├── api/
│   │   ├── posts/           # API برای مدیریت پست‌ها
│   │   ├── users/           # API برای مدیریت کاربران
│   │   └── reset-sample-posts/ # API برای ایجاد نمونه
│   └── page.tsx             # صفحه اصلی
├── components/
│   ├── canvas-editor/
│   │   ├── CanvasEditor.tsx     # کامپوننت اصلی ادیتور
│   │   └── PostEditorDialog.tsx # دیالوگ ویرایش پست
│   └── ui/                 # کامپوننت‌های UI پایه
└── lib/
    └── db.ts               # اتصال به دیتابیس
```

## API ها

### GET /api/posts
获取所有帖子，支持参数:
- `published`: true/false برای فیلتر انتشار
- `archived`: true/false برای فیلتر آرشیو

### POST /api/posts
ایجاد پست جدید:
```json
{
  "title": "عنوان پست",
  "content": "توضیحات",
  "canvasData": "JSON data",
  "background": "#ffffff",
  "blurAmount": 0,
  "published": false,
  "authorId": "user-id"
}
```

### PUT /api/posts/[id]
به‌روزرسانی پست موجود

### DELETE /api/posts/[id]
حذف پست

## نصب و راه‌اندازی

1. نصب پکیج‌ها:
```bash
npm install
```

2. راه‌اندازی دیتابیس:
```bash
npm run db:push
```

3. ایجاد کاربر ادمین:
```bash
curl -X POST http://localhost:3000/api/users
```

4. ایجاد پست‌های نمونه:
```bash
curl -X POST http://localhost:3000/api/reset-sample-posts
```

5. اجرای برنامه:
```bash
npm run dev
```

## تکنولوژی‌ها

- **Frontend**: Next.js 15, React 19, TypeScript
- **Styling**: Tailwind CSS 4, shadcn/ui
- **Database**: Prisma ORM, SQLite
- **State Management**: Zustand, React Query
- **UI Components**: Radix UI, Lucide Icons

## قابلیت‌های آینده

- آپلود تصویر مستقیم
- قالب‌های آماده
- انیمیشن و ترنزیشن
- خروجی تصویر از کانواس
- همکاری چند کاربره
- نسخه‌برداری از پست‌ها